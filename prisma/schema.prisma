model Abstract {
    id          String   @id @default(cuid())
    title       String
    introduction String
    theme       String
    type        String   // 'poster' or 'oral'
    status      String   // 'submitted', 'reviewing', 'approved', etc.
    created_at  DateTime @default(now())
    // ... other fields ...
} 